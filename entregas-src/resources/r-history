matrix.pois<-(rpois(n=200,lambda=10),4)
matrix.pois <-( rpois(n=200,lambda=10), 4)
x.poi<-rpois(n=100,lambda=10) # a vector of random variables from the Poisson distr.
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=21) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=46) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
x.poi<-rpois(n=100,lambda=198) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
x.poi<-rpois(n=100,lambda=108) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
x.poi<-rpois(n=100,lambda=46) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=198) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=108) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=96) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=194) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=168) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=120) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=96) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=114) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=70) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=96) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=47) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
write.csv(x.poi, file = "c:/temp/XPoi.csv")
x.poi<-rpois(n=100,lambda=47) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
x.poi<-rpois(n=100,lambda=96) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
x.poi<-rpois(n=100,lambda=70) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
x.poi<-rpois(n=100,lambda=114) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
x.poi<-rpois(n=100,lambda=20) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
x.poi<-rpois(n=100,lambda=47) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
x.poi<-rpois(n=100,lambda=20) # a vector of random variables from the Poisson distr.
#In case of count data we can use goodfit() included in vcd package
library(vcd) ## loading vcd package
gf <- goodfit(x.poi,type= "poisson",method= "MinChisq")
summary(gf)
plot(gf,main="Count data vs Poisson distribution")
